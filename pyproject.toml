[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mt5-remote-backtest-automation"
version = "0.1.0"
description = "File-driven automation for MT5 backtest and forward-test workflows"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
authors = [{ name = "Rithik Singh" }]

[tool.setuptools]
py-modules = []
